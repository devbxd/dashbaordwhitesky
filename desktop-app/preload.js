// Intentionally empty — the dashboard runs as plain web content with no need for
// native APIs yet. Kept as a placeholder so contextIsolation can stay on if/when
// something here needs to talk to the main process (e.g. native notifications).
